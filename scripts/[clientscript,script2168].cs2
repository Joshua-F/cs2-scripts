// 2168
[clientscript,script2168]
if (map_members = ^false) {
	mes("More advanced sorting options are available on a members' world.");
} else if (%var281 < 1000) {
	mes("More advanced sorting options are available after the Tutorial.");
}
