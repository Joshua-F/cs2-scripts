// 1569
[proc,script1569]()(int)
if (getwindowmode >= ^windowmode_resizable) {
	if (if_hassub(toplevel_fullscreen:sidemodal) = true) {
		return(1);
	}
} else if (if_hassub(toplevel:sidemodal) = true) {
	return(1);
}
return(0);
