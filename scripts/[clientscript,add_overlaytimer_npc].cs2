// 5471
[clientscript,add_overlaytimer_npc](npc_uid $npc_uid0, int $int1, int $int2, int $colour3, int $int4)
if (~script100 = 0) {
	return;
}
if (npc_find($npc_uid0) = true) {
	~add_overlaytimer_npc($int1, $int2, $colour3, $int4);
} else {
	~debug_mes("Overlay Timers: Unable to find NPC for overlay timer <tostring($int1)>.");
}
