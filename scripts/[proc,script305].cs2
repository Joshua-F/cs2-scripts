// 305
[proc,script305](int $int0)
if ($int0 = 1) {
	if_setcolour(0xff981f, worldmap:zoom_level);
} else {
	if_setcolour(^red, worldmap:zoom_level);
}
if_setontimer("script306(clientclock, event_com)", worldmap:zoom_level);
