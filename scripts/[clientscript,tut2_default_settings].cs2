// 3390
[clientscript,tut2_default_settings]
setwindowmode(^windowmode_resizable);
setdefaultwindowmode(^windowmode_resizable);
_3209(1, 1);
if (~on_mobile = true) {
	~camera_do_zoom(320, 320);
} else {
	~camera_do_zoom(400, 400);
}
