// 2422
[clientscript,script2422]
if_sethide(true, boardgames_rank_overlay:draughts_board);
if_sethide(true, boardgames_rank_overlay:runelink_board);
if_sethide(true, boardgames_rank_overlay:runesquares_board);
if_sethide(true, boardgames_rank_overlay:runeversi_board);
if (%varcint808 = 1) {
	if_sethide(false, boardgames_rank_overlay:draughts_board);
	if_settext("Draughts", boardgames_rank_overlay:game_name);
	if_settext(tostring(%var354), boardgames_rank_overlay:rank);
} else if (%varcint808 = 2) {
	if_sethide(false, boardgames_rank_overlay:runelink_board);
	if_settext("Runelink", boardgames_rank_overlay:game_name);
	if_settext(tostring(%var353), boardgames_rank_overlay:rank);
} else if (%varcint808 = 3) {
	if_sethide(false, boardgames_rank_overlay:runesquares_board);
	if_settext("Runesquares", boardgames_rank_overlay:game_name);
	if_settext(tostring(%var648), boardgames_rank_overlay:rank);
} else if (%varcint808 = 4) {
	if_sethide(false, boardgames_rank_overlay:runeversi_board);
	if_settext("Runeversi", boardgames_rank_overlay:game_name);
	if_settext(tostring(%var649), boardgames_rank_overlay:rank);
}
