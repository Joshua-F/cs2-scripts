// 5372
[proc,script5372](stat $stat0)
def_int $comsubid1 = ~script5383($stat0);
def_int $lvl2 = stat_base($stat0);
if (cc_find(interface_729:10, $comsubid1) = ^true) {
	if ($lvl2 < 99) {
		cc_settext(append("Lvl. ", tostring($lvl2)));
	} else {
		cc_settext("");
	}
}
