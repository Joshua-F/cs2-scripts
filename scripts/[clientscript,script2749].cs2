// 2749
[clientscript,script2749](int $comsubid0, int $comsubid1, int $int2)
def_int $int3 = 0;
def_int $int4 = 0;
if (cc_find(interface_900:chathead, $comsubid1) = ^true) {
	if (cc_gethide = true) {
		return;
	}
	if (.cc_find(interface_900:com_74, $comsubid0) = ^true) {
		$int3 = .cc_getmodelangle_y;
		if ($int2 >= 0) {
			if ($int3 <= 256) {
				$int4 = min(calc($int3 + $int2), 256);
			} else {
				$int4 = calc(($int3 + $int2) % 2048);
			}
		} else if ($int3 <= 256) {
			$int4 = ~script686(calc($int3 + $int2), 2048);
		} else {
			$int4 = max(calc($int3 + $int2), 1792);
		}
		.cc_setontimer("script2746(event_com, event_comsubid, $int4)");
	}
	$int3 = cc_getmodelangle_y;
	if ($int2 >= 0) {
		if ($int3 <= 256) {
			$int4 = min(calc($int3 + $int2), 256);
		} else {
			$int4 = calc(($int3 + $int2) % 2048);
		}
	} else if ($int3 <= 256) {
		$int4 = ~script686(calc($int3 + $int2), 2048);
	} else {
		$int4 = max(calc($int3 + $int2), 1792);
	}
	cc_setontimer("script2746(event_com, event_comsubid, $int4)");
}
