// 6970
[clientscript,script6970](component $component0, string $string0, int $clock1, boolean $boolean2)
if (clientclock < $clock1) {
	return;
}
if ($boolean2 = true) {
	~combination_lock_initbutton_confirm($component0, $string0);
} else {
	~combination_lock_initbutton_confirm($component0, $string0);
}
