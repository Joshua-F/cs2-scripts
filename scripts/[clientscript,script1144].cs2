// 1144
[clientscript,script1144](int $int0, component $component1)
def_int $windowmode2 = ~dropdownbox_getselected($int0, $component1);
if ($windowmode2 = 0) {
	detail_fog_on(false);
} else if ($windowmode2 = ^windowmode_fixed) {
	detail_fog_on(true);
}
